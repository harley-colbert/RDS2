# Phase 03 — FRONTEND — GATES

- **CORS** clean: all fetches succeed from :5580 to :5581.
- **State**: selection survives route changes; back/forward navigates correctly.
- **Performance**: lazy-load chunky content; avoid blocking UI.
- **Accessibility**: focus states for keyboard navigation; semantic headings.
