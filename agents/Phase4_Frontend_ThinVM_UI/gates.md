# Gates — Phase 4
- All fetches succeed (CORS OK) from :5580 to :5581.
- Keep state on route changes; enable back/forward navigation.
- Lazy-load large chunk text; avoid blocking UI.
- Add basic accessibility (focus, roles, landmarks).