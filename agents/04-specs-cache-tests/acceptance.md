# Phase 04 — Acceptance

- [ ] Spec extraction returns normalized JSON saved in `extractions` with version.
- [ ] Status/version endpoints working; frontend hydrates from cache.
- [ ] Tests pass locally (`pytest -q`) without warnings.
- [ ] Re-open document triggers no LLM call (observable via logs).