# Gates — Phase 5
- Deterministic spec JSON for same input/version.
- Ensure cache hits on reopen (no LLM call); log to verify.
- Tests include unhappy paths (malformed PDFs, missing headers).
- README includes troubleshooting and curl cheatsheet.