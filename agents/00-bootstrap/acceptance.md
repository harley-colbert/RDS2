# Phase 00 — Acceptance

- [ ] `python run.py` shows logs for both servers on 5580/5581.
- [ ] `curl http://localhost:5581/api/health` → 200 with {"status":"ok"}.
- [ ] Visit http://localhost:5580/ → page renders; no console errors.
- [ ] Ctrl‑C stops both servers cleanly.
